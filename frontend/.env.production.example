# Production build (Vite) — set in Render static build env or frontend/.env.production

# Climeto auth API (login / me / logout)
VITE_AUTH_API_BASE_URL=https://api.climeto.in/api

# Portal URL for logout redirect
VITE_PORTAL_URL=https://portal.climeto.in
